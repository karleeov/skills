---
illustration_id: 02
type: flowchart
style: sketch-notes
palette: inspecto-brand
---

Hand-drawn educational flowchart on soft mint-white paper. Slight wobble on all lines.
Diagram-style ONLY — no photos.

PALETTE OVERRIDE (Inspecto brand):
COLORS: Background (#F0F7F7); Black ink (#1A1A1A) for lines/text; step fills
        alternating Soft Teal (#D6E8E8) and Accent Tint (#C9EBEC); decision or
        owner node in Brand Teal (#006164) with white text; arrows black.
        Color values (#hex) and color names are rendering guidance only —
        do NOT display color names, hex codes, or palette labels as visible text.

TITLE: "/ship — idea to reviewed commits"

Layout: left → right horizontal process, 6 stages.

STEPS (exact labels):
1. "preflight + discovery"
2. "/grill-with-docs" — sublabel "unresolved decisions only"
3. "/to-spec" + "/to-tickets" — sublabel "ticket DAG"
4. "/implement" frontier tickets — sublabel "one at a time"
5. "integration proof"
6. "branch review + endpoint"

CONNECTIONS: Solid hand-drawn arrows between stages. Small note above stage 4:
"fresh worker per ticket". Owner badge on title bar: "/ship owns the run".

BOTTOM takeaway: "Persist the run outside the feature diff"

STYLE: sketch-notes, rounded stage cards, hand-lettered keywords only,
generous white space, clean composition, main flow centered.
ASPECT: 16:9
